{% extends 'base.html.twig' %}

{% block title %}Chopper - Account - Modify password{% endblock %}

{% block content %}
    <h1>Modify Password</h1>

    <a href="{{ path('account') }}"> Modify password </a><br />
    <hr>
    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
    {%  endif %}
    {{ form(form) }}
    <hr>

{% endblock %}
